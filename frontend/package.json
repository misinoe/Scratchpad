{
  "name": "frontend",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "clean": "yarn run rimraf output/**/*",
    "watch": "yarn run parcel watch ./src/*.html --dist-dir ./output",
    "build": "yarn run clean && yarn run parcel build ./src/*.html --dist-dir ./output",
    "help": "yarn run parcel help build"
  },
  "devDependencies": {
    "parcel": "^2.0.0-rc.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.3.5"
  },
  "private": true,
  "dependencies": {
    "pixi.js-legacy": "^6.1.2",
    "socket.io-client": "^4.1.3"
  }
}
