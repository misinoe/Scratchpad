{
  "name": "frontend",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "clean": "yarn run rimraf output/**/*",
    "watch": "yarn run parcel watch ./src/*.html --out-dir ./output",
    "build": "yarn run clean && yarn run parcel build ./src/*.html --out-dir ./output"
  },
  "devDependencies": {
    "@types/socket.io-client": "^1.4.32",
    "parcel": "^2.0.0-rc.0",
    "pixi.js-legacy": "^6.1.2",
    "rimraf": "^3.0.2",
    "socket.io-client": "^4.1.3",
    "typescript": "^3.6.3"
  },
  "private": true
}
